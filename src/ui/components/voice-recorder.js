import {t} from "../../i18n/i18n.js";
export function renderVoiceRecorder(state,{compact=false}={}){
 const speech=state.speech??{},caps=speech.capabilities??{};
 if(!caps.recording)return `<div class="voice-panel voice-unavailable"><strong>${t("recording_unavailable")}</strong><span>${t("recording_unavailable_note")}</span></div>`;
 return `<div class="voice-panel ${compact?"is-compact":""}"><div class="voice-status"><span class="voice-dot ${speech.recording?"is-recording":""}"></span><span>${speech.recording?t("recording"):t("voice_practice")}</span></div><div class="voice-actions">${speech.recording?`<button type="button" class="primary-button compact-action" id="voice-stop">${t("stop")}</button>`:`<button type="button" class="primary-button compact-action" id="voice-start">${t("record")}</button>`}${speech.recordingUrl?`<button type="button" class="secondary-button compact" id="voice-clear">${t("delete_record")}</button>`:""}</div>${speech.recordingUrl?`<audio class="voice-audio" controls src="${esc(speech.recordingUrl)}"></audio><small class="muted">${t("seconds",{n:Math.max(0,Math.round((speech.recordingDurationMs??0)/1000))})}</small>`:""}${speech.error?`<p class="voice-error">${esc(speech.error)}</p>`:""}</div>`;
}
function esc(v=""){return String(v).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#039;");}
